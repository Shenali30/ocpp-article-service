server:
  port: 8081
base-url:
  context: /tech-voice
app:
  host: localhost:8081
logging:
  level:
    com.poc.techvoice.articleservice: INFO
audit:
  user: user-id
security:
  jwt:
    sign-key: aaASWKddlTRRVMs_WEERANvvapotningu_WANWiresharkqqaOOPQUBE
task:
  executor:
    pool:
      core-size: 100
      max-size: 100
    queue-capacity: 1000
spring:
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://host.docker.internal:3306/tech_voice?createDatabaseIfNotExist=true&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root@123
  mail:
    host: smtp.gmail.com
    username: test123@gmail.com
    password: password@123
    port: 587
